{% with messages = get_flashed_messages() %}
    {% if messages %}
        <ul class="Messages">
            {% for message in messages %}
                <li class="Messages__message">{{ message }}</li>
            {% endfor %}
        </ul>
    {% endif %}
{% endwith %}
